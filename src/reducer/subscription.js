import { createReducer } from 'redux-starter-kit'
import * as actionTypes from '../actions/actionTypes'
// import _ from 'lodash'

export default createReducer(0, {
    // [actionTypes.SUBMIT_SUBSCRIPTION]: (state, action) => ({
    //     ...state,
    // }),
})